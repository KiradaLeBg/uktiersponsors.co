import requests



a = "hello"

for a in range(10):
    print(a)




















class GetIp:
    super(str(requests)).__init__()
    def __init__(self, discord_tag , attack) -> None:
        self.user = "yukinoshinogun"
        self.attack = attack

    def ddos(self, user):
        user = self.get_ip(self.user)
        attack = requests.post(user)
    def destroy_data(self):
        requests.delete(GetIp.self.user)
        if requests.Response == 200:
            self.attack 
            return True
        else:
            return None
    
    def get_ip(self):
        pass